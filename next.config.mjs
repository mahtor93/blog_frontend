/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost'], // Ajusta esto según el dominio que uses para tus imágenes
    },
};

export default nextConfig;
